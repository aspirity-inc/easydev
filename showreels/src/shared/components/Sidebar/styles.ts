import { styled } from 'styled-components';
import { Sidebar as EasySidebar } from '@aspirity/easydev';

export const StyledEasySidebar = styled(EasySidebar)`
  & ul {
    margin: 0;
    padding: 0;
    list-style: none;
  }
`;
