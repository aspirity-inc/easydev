import styled from 'styled-components';

import { Input } from '..';

export const StyledCodeInput = styled(Input)`
  width: 24px;
  height: 24px;
  padding: 0;
  border-radius: 4px;
  text-align: center;
  font-weight: 450;
`;
